{{!< default}}

<div class="page-post">
    {{#post}}
        <div class="container-fluid">
            <div class="row section with-border-top">
                <div class="col-xs-12 col-md-offset-1 col-md-3">
                    <h1>{{@blog.title}}</h1>
                    <p>{{@blog.description}}</p>
                </div>
                <div class="col-xs-12 col-md-7">
                    <h5>{{@title}}</h5>
                    {{#if image}}
                        <img src="{{image}}" class="featured-image"/>
                    {{/if}}
                    {{content}}
                </div>
            </div>
        </div>
    {{/post}}
    {{>contact-links}}
</div>