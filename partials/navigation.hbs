<div class="container">
	<div class="row">
		<nav class="navbar">
    		<a href="/" class="navbar-brand"><img src="/assets/image/logo-ucsc.svg"></a>
    		<a href class="navbar-hamburger"><span></span></a>
		    <ul class="navbar-nav">
		        {{#foreach navigation}}
		            <li class="nav-{{slug}}{{#if current}} active{{/if}}" role="presentation"><a href="{{url absolute="true"}}">{{label}}</a></li>
		        {{/foreach}}
		    </ul>
		</nav>
	</div>
</div>
